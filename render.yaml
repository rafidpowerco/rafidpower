# https://render.com/docs/blueprint-spec
# Deploy: connect GitHub repo → New Blueprint → select this file, or Web Service → Docker.

services:
  - type: web
    name: rafidpower
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
